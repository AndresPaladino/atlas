---
description: Auditar consistencia del wiki (orphans, links rotos, Bloom drift, etc.)
---

Activá modo `lint`. Antes de responder, leé `schema/lint-protocol.md`.

Scope (opcional, vacío = todo el wiki): $ARGUMENTS

Aplicá todos los checks del protocolo. Presentá el reporte resumido y ofrecé fix interactivo. Read-only por default — no modifiques nada sin confirmación explícita por finding.
