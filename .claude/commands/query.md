---
description: Responder una pregunta consultando el wiki
---

Activá modo `query`. Antes de responder, leé `schema/query-protocol.md` y `schema/wiki-conventions.md`.

Pregunta del usuario: $ARGUMENTS

Si está vacía, pedí la pregunta. Si tiene contenido, aplicá el protocolo: localizar candidatas en `wiki/index.md`, leer 3-8 páginas, sintetizar con `[[wikilinks]]` y citas, y ofrecer archivar.
